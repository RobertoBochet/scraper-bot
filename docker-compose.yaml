services:
  scraper-bot:
    image: ghcr.io/robertobochet/scraper-bot
    restart: unless-stopped
    environment:
      - SB__REDIS=redis://redis/0
    depends_on:
      - redis
    volumes:
      - "./config.yaml:/etc/scraperbot/config.yaml"

  redis:
    image: redis:7.2-alpine
    restart: always
